# hi
#use with kyori MiniMessage and PlaceHolderAPI

#Placeholders:
#{player} - sender
#{pluginprefix} - is prefix
#{message} - message in /gc
#{groupName} - name of group

prefix: "[<aqua>GroupChat<white>] <gray>"

message-style: "{pluginprefix}{player}: {message}"